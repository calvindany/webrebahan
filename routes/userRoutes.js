const express = require('express');
// const adminController = require('../controller/userController.js');

const router = express.Router();

// router.get('/', adminController.getIndex);

module.exports = router;